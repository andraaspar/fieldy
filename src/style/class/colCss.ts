import { css } from 'emotion/macro'

export const colCss = css({
	display: 'flex',
	flexDirection: 'column',
})

export const colAutoCss = css({
	display: 'flex',
	flexDirection: 'column',
	overflow: 'auto',
})
